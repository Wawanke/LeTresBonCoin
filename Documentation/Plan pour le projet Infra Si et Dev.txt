Plan pour le projet Infra Si et Dev :

Thématique du site : Forum, nom : LeTrèsBonCoin

Faire une chartre de projet

Technos utilisées  : Golang pour le back, html/CSS pour le front, docker pour l'infrastructure, MySQL pour la base de donnée, github pour la mutualisation (https://github.com/Wawanke/LeTresBonCoin/tree/master).

Pour le Dev : 

Figma du html/css pour avoir une idée global du site 
voir fonctionnalités, à faire en prototype (Golang) puis exporter en microservice avec docker

Fonc de base :  authenfication, profil visiteur, création post si connecté, modif profil si connecté

midleware : potentiel pour bloqué le routes disponibles

Pour l'Infra Si :

Infrastructure micro-service :
Pour Docker il faut wsl (regarder) wsl = Windows Subsystem for Linux
utilisation de l'application d'aide au développement docker qui permet de fragmenter les différents services du site : avoir un dock pour l'authentification, avoir un dock pour les posts, pour le profil, pour les actualités etc... Tout en les faisant communiquer (fonctionnemment par micro-service).

On peut faire un service qui fournit les pages en fonction des autorisations que l'utilisateur possède, si c'est un visiteur du site il peut observer seulement sans utiliser les fonctionnalités, mais surtout il ne doit pas avoir accès à certain "endroit du site"

protéger les points d'acces du site, ex. : si une personne arrive sur le site et qu'il n'a pas l'autorisation d'aller sur certaine page de l'arborescence, la page ne doit donc pas être disponible à l'utilisateur, c'est possible en faisant des dock sur docker 


